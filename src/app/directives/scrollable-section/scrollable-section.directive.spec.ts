import { ScrollableSectionDirective } from './scrollable-section.directive';

describe('ScrollableSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollableSectionDirective();
    expect(directive).toBeTruthy();
  });
});
